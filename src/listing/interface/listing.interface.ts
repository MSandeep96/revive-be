export enum ListingType {
  SALE = 'sale',
  TRADE = 'trade',
  RENT = 'rent',
}

export enum RentingPeriod {
  DAY = 'd',
  WEEK = 'w',
  MONTH = 'm',
}

export enum ListingSort {
  NEAREST = 'nearest',
  LATEST = 'latest',
  CHEAPEST = 'cheapest',
}
